allprojects {
    apply plugin: "idea"

    version = "1.0"
    ext {
        appName = "Spitfire"
        gdxVersion = "1.10.0"
        roboVMVersion = "2.3.15"
        gamesvcsVersion = "1.1.0"
        visuiVersion = "1.3.0"
    }
}
